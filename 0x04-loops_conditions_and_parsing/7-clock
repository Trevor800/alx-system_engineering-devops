#!/usr/bin/env bash
# Script displays time for 12 hours and 59 minutes

hour=0

while ((hour <= 12)); do
    minute=1

    while ((minute <= 59)); do
        printf "%02d:%02d\n" "$hour" "$minute"
        ((minute++))
    done

    ((hour++))
done
